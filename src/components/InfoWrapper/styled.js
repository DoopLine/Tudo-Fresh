import styled from "styled-components";

export const Container = styled.article`
  margin-left: auto;
  margin-right: auto;
  max-width: 813px;
  padding-left: 0.6rem;
  padding-right: 0.6rem;
  text-align: center;

  > h1 {
    font-size: 1.7rem;
  }

  > p {
    font-size: 0.7rem;
  }
`;
